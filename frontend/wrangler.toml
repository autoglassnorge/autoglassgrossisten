name = "autoglass-frontend"
compatibility_date = "2026-05-30"
pages_build_output_dir = "dist"

# Custom domains - må konfigureres i Cloudflare Dashboard
# Gå til: dash.cloudflare.com → Pages → autoglass-frontend → Custom domains
# Legg til: app.auto-glass.no
