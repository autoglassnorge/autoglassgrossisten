name = "autoglass-frontend"
pages_build_output_dir = "dist"
production_branch = "main"

# Domains must be configured in dashboard or via API with Global Key
# Current domains: autoglass-frontend.pages.dev, autoglass.finnbilglass.no
